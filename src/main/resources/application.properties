# ============================================
# Server Port Configuration
# ============================================
# Choose ONE of the following options:

# Option 1: HTTP only (no SSL) - Use this for development without SSL
# server.port=8181

# Option 2: HTTPS only (SSL enabled) - Use https://localhost:8443
# Uncomment the lines below to enable SSL:
server.ssl.enabled=true
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=akashcreations2026
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=akashcreationsspringboot
server.port=443

# Option 3: Both HTTP and HTTPS (HTTP redirects to HTTPS)
# Uncomment the lines below:
# server.ssl.enabled=true
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=akashcreations2026
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=springboot
# server.port=8443
server.http.port=8181

# ============================================
# SSL/TLS Configuration (Currently DISABLED)
# ============================================
# To enable SSL, uncomment the SSL settings above and comment out server.port=8181
# See SSL_SETUP_GUIDE.md for detailed instructions
# ============================================
#jdbc:mysql://localhost:3306/<your database name>?useSSL=false
spring.datasource.url=jdbc:sqlserver://akashss.database.windows.net:1433;database=free-sql-db-2659319;user=akash@akashss;password=Sakshi@599234;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
spring.datasource.username=akash
# update your password as per your mysql password
spring.datasource.password=Sakshi@599234
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.SQLServerDialect
spring.datasource.platform=mssql
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.main.allow-circular-references=true
#Rest of these should be the same if any problem occurs check your libraries version
#also check your maven libraries version

# Memory optimization settings
# Connection pool settings - reduce max connections to save memory
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Hibernate/JPA memory optimizations
spring.jpa.properties.hibernate.jdbc.batch_size=10
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true
spring.jpa.properties.hibernate.query.plan_cache_max_size=64
spring.jpa.properties.hibernate.query.plan_parameter_metadata_max_size=32
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true

# Disable unnecessary features to save memory
spring.devtools.restart.enabled=false
spring.jpa.open-in-view=false

# Actuator memory optimization
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized

jwt.secret=javainuse